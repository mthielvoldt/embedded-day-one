#ifndef SERIAL_H
#define SERIAL_H

/**
 * Contains a subset of the #defines typedefs and function declarations that are in the acutal serial.h
 * This file narrows the scope of what we need to provide fake implementations for to just those
 * functions that are actually used by the code under test.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>


#endif // SERIAL_H
